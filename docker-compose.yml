version: '3.8'

services:
  app:
    build: .
    container_name: rate-limit-lab
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app/app
      - db_data:/app/data
    restart: unless-stopped

volumes:
  db_data:
